# AGENTS.md — DCITC Static Site

## Objective
Production-quality static website for Dhaka College IT Club (DCITC) at `/home/shofol/Projects/test2`. Zero-dependency Node build pipeline outputs plain HTML/CSS/JS to `public/`, structured Hugo-compatibly so it can be migrated to Hugo later. The user iterates on UI/UX details; always verify changes in a headless browser and confirm geometry numerically (this model cannot view images).

## Build & Commands
- Build: `cd /home/shofol/Projects/test2 && node scripts/build.js` → writes `public/` (templates + CSS/JS concat bundles).
- Serve: `python3 -m http.server 8080 --directory public` (or similar) at `http://localhost:8080`.
- JS is bundled at build time: `static/js/*.js` → `public/js/app.js` (concat order in build.js `JS_FILES`). CSS likewise → `public/css/main.css`. **Rebuild after editing any static asset**; the dev server does NOT hot-reload.
- Test tooling in `/tmp/opencode/` (survives reboot? no — recreate if missing):
  - Playwright: `/tmp/opencode/node_modules/playwright-core`; Chromium EXE `/home/shofol/.cache/ms-playwright/chromium-1234/chrome-linux64/chrome` (launch with `args: ['--no-sandbox']`).
  - `shoot.js` — 19 screenshots to `/tmp/opencode/shots/` (uses `addInitScript` to set `localStorage['dcitc-theme']`).
  - Regression/geometry scripts: `car-test2.js`, `ring-check.js`, `ring-cycle.js`, `ring-fit.js`, `ring-mobile.js`, `foot-*.js`, `gap-probe.js`, `pad-probe.js`, `frost-check.js`, `frost-shot.js`, `frost-sample.js`.
  - Verify by pixel sampling screenshots with a small inline Python PNG decoder (see `frost-sample` flow) when color/opacity matters.

## Architecture
- `scripts/build.js` — template engine + site builder. Key functions: `tokenize`, `renderTokens`, `evalCond`/`condVal`, `splitArgs` (paren-aware arg splitting; quotes inside parens preserved), `evalVal`, `resolve`, `compare`, `truthy`, one-arg helpers (`fmtYear`, etc.), `navFor(page)` computes `isActive`. Guards: depth >60 or iterations >1M throw.
- Template syntax: `{{ .path }}`, `{{ each .arr }}...{{ end }}`, `{{ if eq/ne/lt/gt/le/ge ... }}`, `{{ if not ... }}`, `{{ if or (eq .a "x") (eq .b "y") }}` / `and`, `{{ else }}`, `{{ include "partials/x.html" }}`, `{{ helper path }}`.
- Engine history (fixed bugs — do not regress):
  - `or`/`and` args must be evaluated as conditions — `condVal()` strips wrapping parens and routes condition-looking args to `evalCond`, bare paths to truthy values.
  - `splitArgs` must group `(...)` segments (paren-depth tracking) and preserve quotes inside paren groups; otherwise `(eq .role "X")` splits into `(eq`, `.role`, `X)` and the stray `(eq` string makes `or` always true.
- Layout: `main[data-horizontal]` = flex row (`overflow-x: auto; overflow-y: hidden`) on ≥900px; vertical below. **Every direct flex child needs `flex: 0 0 auto`** or it gets flex-shrunk: `.site-nav` and `.sec-footer` both had this bug (nav squashed to ~35px; footer crushed 1382→522px with overlapping text). `.sec` already has it.
- Breakpoint: 900px (hx-body class toggled by JS). `main.hx` display:block + `body:not(.hx-body)` rules handle vertical mode. NOTE: `hx-body` class may persist below 900 (CSS switches layout); scope hx-only rules with `body.hx-body` AND `@media (min-width: 900px)` to be safe.
- Sections: `.sec { min-height: 100%; flex: 0 0 auto }` — they GROW beyond viewport height if content demands, but `main.hx` clips vertically (`overflow-y: hidden`), so tall content is cut on short viewports. Keep sections ≤ ~viewport height at 650px tall viewports.
- Nav: `position: sticky; top: 0` + `flex: 0 0 auto`; content starts below it. Mobile menu is CSS-visibility toggled (NO `hidden` attribute — was removed).
- Theme: dark-first (`--bg #0c0f13` etc.), light theme in vars; accent `#4fd1a5`/`#157a5b`; inline head script sets `data-theme` pre-paint; persisted `localStorage('dcitc-theme')`; `prefers-reduced-motion` disables animations.
- Scroll: pure smooth manual horizontal scroll (easing 0.16) — snap was deliberately removed. `.hx-status` bottom-left readout was deliberately removed (markup/CSS/JS all gone).
- Reveal animations: `reveal.js` has a rAF safety net revealing in-view/already-past elements.
- Parallax `[data-plx]`, float animations, anime.js vendored at `static/js/vendor/`.
- Content: `src/data/*.json` (site, nav, team, projects, resources, achievements), `content/blog/*.md` (Tech Journal) + `content/events/*.md` (events calendar) + `content/gallery/*` (photo strip — pure image files) — content/<type> authored in its own format, `src/pages/*.html`, `src/partials/{head,header,footer,scripts}.html`. Directory-style output: `public/<page>/index.html`, items at `public/<type>/<slug>/index.html`. Article pages (`article.html`) intentionally have no footer.
- **Markdown blog + events** (replaced mock `src/data/posts.json` AND `src/data/events.json`, both deleted): one `.md` per item under `content/blog/` / `content/events/`; filename IS the slug. Shared pipeline in build.js: `escHtml()` + `marked.use()` (gfm; code-fence renderer override emits `<pre class="code"><code>`) + `loadMarkdownPost()` live in a "markdown pipeline" block ABOVE both loaders — keep that order or events render without the pre.code override. Frontmatter parsed by `js-yaml` (the project's only npm deps, with `marked`).   Blog: title+date required. Events: title+date+status required (status ∈ upcoming|ongoing|past, validated — drives badges/deck/archive); program[{time,title}], speaker{name,role}, resources[], register, location/duration/level stay STRUCTURED frontmatter because the deck/timeline render them as components; optional markdown body → event page About plate (`{{ .item.contentHtml }}` in `.md-body.ev-brief`, falls back to short `description`). Both: draft:true dropped before anything runs, sorted date-desc (tie slug asc), codes ART.xx/EVENT.xx assigned AFTER sort (01=newest — deck's pre-rendered active card is now the NEWEST upcoming, not json-order). Engine does NOT escape values → raw HTML injection works. `.md-body` scoped CSS in 08-pages.css covers ol (decimal-leading-zero counters), tables, img, hr, inline code, links; `.ev-brief` variant adds typography for the event plate without touching journal pages. Build fails loudly on missing fields or URL-unsafe filenames.
- **Vertical reading pages** (2026-08-24): BOTH article.html AND event.html are vertical — `<body>` has NO `hx-body`, `<main>` NO `data-horizontal`; body:not(.hx-body) rules in 04-layout.css stack sections, horizontal.js falls back to initVertical(). Event single KEEPS its footer partial; article intentionally has none. Everything else (events index incl. deck, project singles) stays horizontal.

## Leadership Carousel (About page) — circular ring
- Markup `about.html`: `.car[data-carousel]` > `.car-track[data-car-track]` + SVG arrow buttons `[data-car-prev]`/`[data-car-next]`; `.car-slide` wrappers around `.profile` cards. Two filtered `{{ each .team }}` loops: President (eq), then `if or (eq .role "Vice President") (eq .role "General Secretary") (eq .role "Technical Lead")` → 4 cards total.
- JS `initCarousel()` in `static/js/pages.js`: computes signed distance `sd` per slide; assigns `pos-front` / `pos-right` / `pos-left` / `pos-back-r` / `pos-back-l` classes (back-r/l by sd sign). `is-active` on front. Wrap-around cycling; resize re-centers. (Old linear version used `track.clientWidth` — carousel now class-based, that bug is moot.)
- CSS `08-pages.css`: slides absolutely positioned `top/left: 50%` + transform per pos class (`.pos-right` = `translate(calc(-50% + 62%), calc(-50% - 3%)) scale(.76) rotateY(-35deg)` with `perspective: 1300px` on track; `.pos-back-r` = `translate(calc(-50% + 15%), calc(-50% - 30%)) scale(.45) rotateY(-20deg)` opacity .35; front z 5, sides z 2, back z 1). Track `height: clamp(390px, 42vh, 450px)`. Back card peeks ~17-27px above front. Mobile (≤560px): width clamp(200px, 62vw, 240px), side offset 36%, car padding-inline 1rem. Section must stay ≤ ~650px viewport height — geometry was tuned (compact): front card ~250×394, link rows tight.
- `[data-section="Leadership"] .sec-head` margin tightened.
- Ring direction: next = right card comes to front; order front → right → back → left → front.

## Footer
- `.sec-footer`: `flex: 0 0 auto; width: min(80vw, 1280px)` (hx only; full width below 900 and on vertical pages via `body:not(.hx-body)` width:100%). **No margin-inline** — the trailing margin created a "gap after footer" at max scroll; footer must end flush at page edge.
- Internal padding: `clamp(1.5rem, 4vh, 3rem) clamp(1.5rem, 3vw, 3rem)`; content vertically centered (`justify-content: center`); block gap 1.5rem.
- Frost glass: `backdrop-filter: blur(18px) saturate(150%)` (+ -webkit-) over `linear-gradient(180deg, color-mix(in srgb, var(--bg-elev-2) 44%, transparent), color-mix(in srgb, var(--bg-elev) 26%, transparent))`, with SVG `feTurbulence` noise (feColorMatrix dims it: gray alpha 0.09) layered on top; inset top hairline (`box-shadow: inset 0 1px 0 color-mix(in srgb, var(--ink) 8%, transparent)`). Blur verified by A/B pixel sampling with injected high-contrast stripes (edge energy frost=4.8 vs nofrost=18.5 → ratio 0.26); the real backdrop is too dim to measure through — don't re-audit against it.
- Link columns: statement (`foot-id`, left) + `.foot-cols` 2×2 grid (ORGANIZATION/WORK top, EVENTS/EXTERNAL bottom) on the right; `.foot-cols` = `repeat(2, minmax(0,1fr))` gap 1.25rem; links line-height 1.45, ul gap 0.35rem.
- `.foot-wordmark`: absolute, `inset-inline: 0`, `top: 50%`, translateY(-50%), `font-size: 38cqi` (footer has `container-type: inline-size`) — spans ~100% of footer width; hollow text (`-webkit-text-stroke: 1.25px color-mix(in srgb, var(--accent) 70%, transparent)`, color transparent — slight accent cast, ~4% chroma-shifted px in the wordmark band both themes), `opacity: 0.12`, `z-index: 0`, `pointer-events: none`. Content blocks `.foot-top`/`.foot-bottom` need `position: relative; z-index: 1`.
- Media: ≤899px foot-top stacks column, grid stays 2 cols; footer width 100%, margin 0.

## Current State (all verified + screenshots in /tmp/opencode/shots/)
- Build clean; engine or/and/splitArgs fixed; carousel ring live with 4 cards (Tanvir, Farzana, Abdullah, Arif), cycling, centered (0px), mobile OK.
- Footer: frost + faint full-width wordmark + 2×2 grid + inner padding + no trailing gap.
- 19 screenshots refreshed. Server: `http://localhost:8080`.
- Whole codebase documented (headers + inline comments in build scripts, all JS/CSS, partials, pages; data files documented in README.md "Data file reference").
- **CRITICAL template rule: NEVER put `{{`/`}}` inside HTML comments** — the engine tokenizes comments too. A literal `{{ if .group }}` in a comment once swallowed the entire header (its `end` matched real markup). Interpolation (`{{ .x }}`) in comments is ugly-but-harmless; conditionals are page-breakers.
- Filter bug fixed in `pages.js initFilters()`: items selector must be `selector + ' [data-filter]'` (filterable descendants), NOT the container itself — the old code set display:none on the whole `.res-list`/`.masonry` on first category click. **Resources Browse section later REMOVED** (user request): filter chips now gallery-only; resources search input lives in the shelf `.sec-head.res-head` right cell (width min(300px,34vw), wraps full-width ≤899px). Verified: search "linux"→4/30 cards, no overflow desktop+mobile.
- Test tooling recreated after wipe: `/tmp/opencode/shoot.js` (19 shots) + `verify-docs.js`/`verify2.js`/`verify3.js` regression probes. Playwright reinstalled at `/tmp/opencode/node_modules/playwright-core`.
- About-page leadership: circular ring carousel REMOVED → 4 `.profile` cards inline in `.sec-leadership` (grid 4-col → 2-col ≤899px/≤560px, mirrors `.team-grid`). `initCarousel()` deleted from pages.js; all `.car*` CSS removed. Home "What we do" rows compacted (`.do-row` padding 0.7rem).
- Home featured-projects cards COMPACTED (08-pages.css, scoped `[data-section="Featured projects"]` — blog shares `.card--media`, must keep 16/9): thumb aspect 2/1 then 12/5 (172px @1440), body gap 0.35rem + padding 0.75/0.9/0.85rem, foot padding-top 0.4rem, taglines line-clamped to 2, grid gap 1rem ≤899px. Cards now ~397px tall (was 443); sections still fit 650px viewports.
- Home CURATED RESOURCES compacted + hard-capped at 4 (2026-08-24): scoped `[data-section="Resources"]` (safe — resources page labels its sections "Overview"/"Knowledge base"); card gap .45rem, padding .9/1rem, foot padding-top .45rem, list gap .85rem, desc clamped to 2 lines (`-webkit-line-clamp`) → uniform ~167px cards (was 194–234). **The 4-card cap is now the `featured.resources` list in src/config/site.json itself** (was `featuredResources.slice(0,4)` in build.js — removed; per-file `featured` booleans removed too). Never grow the home grid beyond what the tuned CSS expects, and keep it to 4.
- Events "Upcoming" = collapsing card deck (spec in sections.md): `ul.evx[data-evx] > li.evx-item > article` with strip label + body on a PLAIN `--bg-elev` surface — **cover imagery removed 2026-08-24** (looked wrong in light mode); event `cover:` field and event-slug plate seeds deleted from build.js, `.evx-item img` rules gone. build.js sets `first:true` on first upcoming event → ships `data-active="true"` pre-rendered. JS `initEventDeck()` (pages.js) rewrites `grid-template-columns` to `10fr 1fr…` on pointerenter/click/focusin — the animatable track sizes ARE the motion (no anime.js). CSS `.evx` block in 08-pages.css themed via site tokens; height clamp(280px,40dvh,400px) fits 650px viewports. **A11y gotcha: inactive `.evx-body` must hide via opacity+pointer-events, NOT visibility:hidden — hidden CTAs are unfocusable and keyboard users could never expand a card.** Verified by `/tmp/opencode/evx-verify.js` (initial/hover/focus/short-viewport/mobile) + evx-dark/light/mobile shots.
- **CENTRALIZED CONTENT CONFIG (`src/config/site.json`, 2026-09-03)**: featured/curated selections + ordering are centralized, referenced by slug, NOT hardcoded in templates or per-file `featured` booleans (those are gone). `featured.projects` → home grid + projects deck (same trio, both pages); `featured.posts` → home "Tech Journal" + blog Featured; `featured.resources` → home resources (home-only); `home.events` → home "Upcoming events" ONLY (curated; the events page deck keeps ALL upcoming via `upcomingEvents`). `resolveRefs(list, coll, label, key)` in build.js resolves slugs against the loaded collections, preserves CONFIG order (so it drives ordering), and **throws a clear build error (with available keys listed) on any reference to a nonexistent slug — a typo or deleted file can never silently drop content**. `featured.projects` is capped at 3 (home grid + deck are tuned for exactly three; >3 throws). Resources gained a `slug` field in resources.json for stable config keys. Status-derived subsets (`upcomingEvents`/`pastEvents`/`activeProjectsCount`) remain status/date-driven, not config. `homeEvents` is exposed to all template scopes; index.html section 05 loops it. Docs: README "Centralized content config" + way.md. To change a featured item/order: edit the array in src/config/site.json and rebuild.
- **Vertical scroll zones** (`[data-vscroll]` on section's `.sec-in` + `[data-vzone]` on the scrolling child, resources shelf): head chrome stays PINNED (island = flex column, `.sec-head {flex:0 0 auto}`), only `[data-vzone]` (.res-list) scrolls (`flex:1 1 0; min-height:0; overflow-y:auto; overscroll-behavior:contain`, thin themed scrollbar). horizontal.js `onWheel` returns early ONLY when `e.target.closest('[data-vzone]')` — wheel over cards browses vertically, over title/search/everything else pans horizontally. **Four traps hit**: (1) `.sec .sec-in {overflow:visible}` in 06-horizontal.css out-specifies bare `[data-vscroll]`; (2) `.sec > .sec-in {margin-block:auto}` defeats align-self stretch; (3) flex implicit `min-height:auto` floor makes .sec grow instead of cap — needs `.sec:has(>[data-vscroll]) {min-height:0}`; (4) island itself must NOT be the scroller if head should stay pinned. Verified by `/tmp/opencode/vscroll-verify2.js` (card/head/search/bottom-containment/last-card/outside/search/mobile).
- **Page fluid — whole-page ASCII backdrop on HOME (replaces the deleted hero globe + the interim hero-scoped fluid; three.js vendoring fully removed)**: faithful port of javierbyte/fluid-triangle (FLIP solver from Ten Minute Physics, MIT) in `static/js/fluid-triangle.js` (concatenated into app.js via JS_FILES, registers `DCITC.fluidTriangle`, `main.js` calls `init()`; bails when `[data-fluid-triangle]` absent → home only). Markup: `<div class="page-fluid" data-fluid-triangle aria-hidden="true">` between progress bar and `<main>` in index.html; module creates `.render` inside. CSS `.page-fluid` block in 08-pages.css: `position:fixed; inset:0; z-index:-1` → above .bg-grid(-2), below all content; **NO background of its own** — site --bg shows through instead of upstream's black (user spec: gray in dark, white in light); chars `color: color-mix(in srgb, var(--accent) 24%, var(--ink-dim))` (slight accent cast) + layer dimmed via `--fluid-opacity` (.13 dark / .18 light — the tuning knob; user wants light kept at .18 while dark goes lower). **DESKTOP ONLY**: `(min-width: 900px)` matchMedia gate in the module (live — crossing 900 boots/stops+clears for tablet orientation flips) AND `display:none` on `.page-fluid` ≤899px; below 900 nothing mounts and zero listeners bind. `.render`: white-space:pre, letter-spacing .4em (= one cell w/ JetBrains Mono), weight 700, font-size/line-height set inline by JS.
  - **PERF RULE (do not regress — first attempt was "heavy")**: loop stays upstream-shaped — at most ONE `fluid.simulate()` + one render per RENDERED frame; rendered frames capped ~30fps by SKIPPING whole frames (`FRAME_MIN_MS`, a skipped frame does no sim work — no catch-up debt, which is what thrashed CPU in the first "heavy" attempt). NO accumulator, NO quality governor/rebuilds. Render via `textContent` not `innerHTML` (plain ASCII → no HTML parse per frame). Headless-software ≈30fps ceiling; real browsers idle far below budget.
  - **THE CIRCLE BUG (do not "fix" the typo back)**: upstream `setObstacle()` carried a Ten Minute Physics leftover that stamps a CIRCLE of solid cells into the grid, but it read `f.numX/f.numY` (undefined) so on javierbyte's published site it never ran — his demo's water only ever collides with the moving TRIANGLE via `handleParticleCollisions()`. Restoring the loop with correct field names (first pass) resurrected a growing solid disc mid-screen ("why does it turn into a circle?"). Fix: `setObstacle()` keeps ONLY obstacle position tracking; no grid stamping.
  - Interaction: listeners on WINDOW (layer itself pointer-events:none): mousedown/move/up stir anywhere on the page; `<main data-horizontal data-hx-nodrag>` on home tells horizontal.js to skip mouse-drag panning (wheel/keys/touch still pan). Touch listeners passive, never preventDefault. Device-motion permission requested ONLY inside first click/touchend gesture, silent on deny, orientation-compensated (screen.orientation.angle + window.orientation fallback), gravityVector is module-local (not window.global).
  - Dropped upstream dead weight: never-enabled canvas pixel renderer + its `<canvas>`, setObstacle's circle-stamping body (see above), per-cell-per-frame dictionary re-sorting (precomputed RENDER_DICTS once), p/m pause hotkeys, console.log spam, `toggleStart`, unused scene fields (`showObstacle`, `obstacleVelX/Y`). Resize = debounced soft `setupScene()` rebuild (upstream did location.reload()). prefers-reduced-motion = 60-frame async settle burst then loop halts permanently, no listeners.
  - Verified `/tmp/opencode/fluid-verify.js` (27 checks: desktop set as before + mobile display:none/zero-render/quiet + gate crossing up boots / down stops-and-clears; reduced-motion quiescence probe needs >600ms-stable window — 30fps cap makes brief windows falsely static) + `fluid-shots.js` (throws if mobile renders) + `fluid-sample.py`. Mobile pixel TEXTURED verdict post-change = page content, NOT fluid (DOM asserts are the proof). Shots `/tmp/opencode/shots/pf-*.png`.

- **UI trims + consistency pass (2026-08-24, post-commit 5d500af)**:
  - Footer frost: `backdrop-filter` blur(18px) saturate(150%) added (see Footer section); wordmark stroke accent-tinted.
  - Fluid: DESKTOP ONLY (900px matchMedia gate, live start/stop), ~30fps whole-frame skip cap (`FRAME_MIN_MS`), `textContent` render, opacity lowered to .38/.25 then .30/.18, then .26/.18, finally .13/.18 + a 24% accent color-mix on the chars after ink-dim was brightened (see Page fluid bullet).
  - Hero meta texts removed from HOME hero — `.hero-meta` now contains ONLY the SCROLL hint, and that is `display:none` ≤899px (touch users don't wheel-scroll; comment in 08-pages.css explains). About-page facts row was removed separately below.
  - Legibility halos (bg-colored text-shadow under dim home text) were ADDED then REMOVED per user request — do not re-add without asking.
  - **`.intro-meta` stat rows REMOVED from ALL seven subpages** (about/achievements/events/gallery/projects/resources/team) + `.intro-meta` CSS deleted. This orphaned build helpers `totalMembers` and `sumLen` — both now DELETED from build.js (README helper list updated again); verify helper liveness against built output, not comments.
  - Blog headline was small vs every other page: bespoke `.jrnl-head` clamp(…,4.5rem) replaced with standard `h-display intro-title` classes (136px @1440 like about); `.jrnl-head` rule deleted. Keep intro headlines on the shared scale.
  - **Blog "Start here." featured cards COMPACTED (2026-08-24)**: were full-width stacked `.card--media` — 1278×940 each (16/9 thumb = 718px), section 2090px in an 800px viewport. Now wrapped in `.jrnl-cards` (2-col, 1-col ≤899px) + compact overrides scoped `[data-section='Featured']` in 08-pages.css right after the "Featured projects" block (exact-match attribute selector — does NOT hit "Featured projects"; thumb **3/1**, body gap .3rem/padding .65/.85/.7, subtitle 2-line clamp, foot padding-top .4rem). Inline `style="font-size:var(--fs-4)"` dropped from card titles → default fs-3 like everywhere. Result: **631×392 cards**, content ends 658px from section top (section rect is 744 only because of `.sec {min-height:100%}`); mobile 342×304. Verified `/tmp/opencode/blog-baseline.js` + `blog-mobile.js` + site-smoke 30/30.
  - **Radius/padding pass (2026-08-24, user request)**: `--r-md` 10px→**6px** ("less round but slightly round" for every card/plate/note box) and `--r-lg` 16px→10px in 01-vars.css; `--r-sm` stays 6px (tags/inputs/inline code). Padding up on the text-bearing bordered components only: `.plate` clamp(1.25rem,3vw,2.25rem)→clamp(1.6rem,3.5vw,2.75rem), `.article-note` 1.1/1.2→1.4/1.6, base `.card` 1.35/1.35/1.5→1.5/1.5/1.6 (+~4px height on non-media cards; the compact scoped variants — home Featured projects/Resources, blog Featured — override body padding so tuned geometry is untouched). Verified: computed radii 6px, home tuned sections still ≤650px viewports (594px @1440×650), no mobile overflow, site-smoke 30/30.
- **Dead-code pass + formatting (2026-08-23)**: removed `forherotest.html` (stale three.js experiment); unused build helpers (`lower/pad2/join/plural/cap/add`; `totalMembers`/`sumLen` removed in the SECOND pass after intro-meta rows went away — team.html:35 had used totalMembers; grep-only audits miss interpolated helpers, so verify against built public/ output); `data-plx` parallax machinery in horizontal.js (zero template users; .bg-grid drift KEPT); `drawLines()` + `.draw-line` CSS (no markup); `.amb*` blobs + their `floaty`/`glow-pulse` keyframes; `.term-bar/.term-body` family (`.term` itself is LIVE — inline mono labels); unused utilities `.h-3/.ord/.sec--narrow/.sec--auto/.split--rev/.ticks-x/.btn-ghost-muted/.ach-year/.proj-hero-meta/.single-head/.res-pick+.rp-*`; stale LEADERSHIP CAROUSEL doc block in 08-pages.css. `status--upcoming` looks dead to grep but is template-interpolated — do not remove. Fluid leftovers: `X_RESOLUTION`, `scene.frameNr`, `showObstacle`, `obstacleVelX/Y`. Audit script: `/tmp/opencode/css-audit.py` (class/keyframe usage vs src+js). Formatting: prettier 3 at `/tmp/opencode/node_modules/.bin/prettier --single-quote --print-width 100 --tab-width 2` over scripts/*.js, static/js/*.js (NOT vendor/), static/css/*.css; **templates (src/**/*.html) deliberately excluded** — prettier mangles `{{ }}` engine syntax.
- **Full-regression harness (2026-09-02)**: `/tmp/opencode/regress.js` — all 32 public routes (+ blog/events/projects singles) at desktop+mobile; checks HTTP 200, page/console errors, **user-facing horizontal scroll** (mobile: `window.scrollTo(120,0)` then read `scrollX > 0` — NOT raw `scrollWidth`, which still reports clipped content), broken images, nav presence. Skip nav-expectation for `/funkystuff/*.html` (standalone games, no nav). Current: **ALL PASS**.
- **Mobile overflow fix (2026-09-02)**: `<main>` below 900px had `overflow: visible`, so a long terminal line on `/join/` (`.term` at src/pages/join.html:146) pushed `main.scrollWidth` past the viewport and let the page swipe sideways. Fixed in 04-layout.css mobile block: `main.hx { overflow-x: hidden; overflow-y: visible }` — mobile stacks sections vertically and never pans horizontally, so clipping x is safe. NOTE: `main.scrollWidth` can still report > viewport after this (clipped content is measured, not rendered) — the real signal is whether the *user* can scroll sideways, which `regress.js` checks.
- **Dead CSS (2026-09-02, safe + byte-verified)**: removed `.field` form rules (07-components.css — the ONLY form is the `search-field` on resources, which is self-contained; contact has NO form) and `.pager .dim` (08-pages.css — no `.dim` element in any pager). Audit caution: `.stack` base MUST stay (`stack--lg` only sets `gap` and inherits the flex column from `.stack`); `.menu-scrim`, `.render`, `.ach-tab-*`, `.code` (build emits `pre.code`), `.status--upcoming` (template-interpolated), `.no-js`, `.team-grid*` are all LIVE despite looking dead to a naked class grep — several are created via `className = '...'` in JS which naive audits miss. Verify dead-code against built `public/` output, not src.
- **Regression tooling (recreated after wipe)**: `/tmp/opencode/fluid-verify.js` (27 checks), `/tmp/opencode/site-smoke.js` (30 checks: all pages load clean, gallery filters, resources search, event deck via `.evx-item` hover — collapsed article is clipped so hover the li, theme toggle, mobile menu via `[data-menu-toggle]` + Escape), `/tmp/opencode/blog-baseline.js` + `blog-mobile.js` (blog featured geometry), `/tmp/opencode/fluid-shots.js` + `fluid-sample.py` (pixel sampling).

- **Funkystuff library page (2026-08-24, v2 — was a card-grid launcher)**: `funkystuff/*.html` in project root = toy/game files; **`src/data/funkystuff.json` is the listing manifest** (one entry per game: required `file`+`title`+`dept`, optional `by`; JSON order = row order). Nav entry "Funkystuff" lives in the **More dropdown** (`nav.json`, code 11, group `more`). Page is **VERTICAL** (`pages.json horizontal:false`, body no hx-body, main no data-horizontal — article/event pattern) with NO intro headline — just a doc-style filter bar (`.gal-filters` chips, `data-filter-group=".fs-list"`, chips = All + unique depts via build-exposed `funkyDepts`) above a **divided list reusing `.do-list`/`.do-row`** (home department rows): `.fs-row` link per entry with row number (build pads `n`), title, mono dept, "by …", ↗ arrow; hover = accent bg nudge + accent title. Rows open games in a new tab. build.js: scans folder (URL-safe validation, copies ALL html verbatim to public/funkystuff/), merges manifest (throws on missing file / empty title/dept; WARNS on unlisted scanned files), exposes `funky`+`funkyDepts` in BOTH scopes. Games stay standalone pages outside the template engine; site-owned bits = vendored `/js/vendor/three.min.js` r128 + `/js/vendor/anime.min.js` 3.2.1. **Pre-existing line-runner.html dt bug FIXED**: upstream clamped dt top-only; regressed rAF timestamps → negative dt → negative skyPhase → `SKY[-1]` crash at sky lerp (~654); fix clamps both sides `Math.max(0,…)` (comment in file). Favicon 404 fixed via `/img/favicon.svg`. Verified `/tmp/opencode/funky-verify.js` 31/31 (layout/nav/manifest fields/filter/geometry/game incl. gameplay start/mobile wrap), site-smoke 30/30.

- **TEAM = batch-aware template pages (2026-09-02)**: `src/data/team.json` is `{ "current": "27", "batches": { "27": {label, groups[]}, "26": {...}, ... } }`. The build renders the SAME `team.html` once per batch to `public/team/<id>/index.html` (plus `team/index.html` = current batch), so every batch page looks identical — **adding a batch = add a `"26"`/`"25"`/… key to team.json and rebuild**. Each batch has ONE `groups[]` entry named "Executive Committee" holding ALL members (no per-function subdivisions — every member is on the exec committee), so there is exactly one card section per batch. Engine scope (buildPage + per-batch pages): `team` = current batch groups (about-page backward compat), `teamBatches` = {id→groups}, `currentBatchId`, `activeBatchId`, `batchesList` = [{id,label}]. per-batch code assigned `TEAM.01…`. Group gets a `many: (members.length > 4)` flag from build.js.
  - **Floating batch switcher** (`[data-batch-switch]`, bottom-right square-with-rounded-corners FAB — `border-radius: var(--r-md)`, NOT a circle): `initBatchSwitch()` in pages.js toggles `.batch-switch-menu.is-open`; menu (DOM-first so it opens upward) lists batches via `{{ each .batchesList }}`, `.is-active` from `{{ if eq .id $.activeBatchId }}`, current cohort `.batch-switch-current` ("now").
  - **Long scrollable card strip (≥900px)**: the exec section with `many` renders `[data-team-strip]` on `.sec-in` + a `.team-strip` wrapper (`[data-vzone]`, so horizontal.js onWheel hands through) around a `.team-grid--many`. Cards keep a fixed `grid-auto-columns: 17.5rem` and the strip scrolls HORIZONTALLY (`overflow:auto`, `width:max-content` grid, no height-constraint so it NEVER re-flows cards into multiple columns) — NO vertical-stagger reveal on the strip (the `translateY(18px)` overflowed + hid off-screen cards; override forces `opacity:1; transform:none`). `data-team-strip` CSS: flex column, pinned `.group-head`/`.prose`, `.team-strip { flex:1 1 0; min-height:0; min-width:0; align-self:stretch; margin-block:0; overflow:auto }` + `.sec:has(> [data-team-strip]) { min-height:0; min-width:0 }`. **Vertical fallback scroll**: if a short viewport can't fit a 454px card, the strip scrolls vertically so cards never clip into `main.hx`'s `overflow-y:hidden` ("cards under the page" bug). At ≥700px viewports no vertical scrollbar appears.
  - **REAL PHOTOS**: drop `content/team/<batch>/<seed>.<ext>` (jpg/png/webp) where seed matches a member's `seed` — build auto-detects via `scanTeamPhotos()` and swaps the avatar `<img>` to `/content/team/<batch>/<file>`, copying the file to public. No team.json edit needed.
  - **Engine bug fixed**: `resolve()` split `$.x` into `['','x']` (never worked); corrected `$.` to `slice(2)` — `$.rootField` interpolation + `eq .id $.x` now resolve. Verify against built pages (team page is the first real `$.` user).

## Next Move (if continuing)
- Nothing pending. If fluid feels too strong/weak, tune `--fluid-opacity` in 08-pages.css (.13 dark / .18 light); frame budget knob is `FRAME_MIN_MS` in fluid-triangle.js. After any CSS/JS change: rebuild, rerun `fluid-verify.js`; never assume image reading is possible.
- Funkystuff: drop more `.html` files into `funkystuff/` AND list each in `src/data/funkystuff.json` (`file`/`title`/`dept`/optional `by`) to grow the shelf — unlisted files are copied but not shown (build warns); manifest entries referencing missing files fail the build. Keep games self-contained; site-owned assets are the vendored three.js/anime.js paths only.
- **Gallery = folder-driven photo strip (2026-09-01, v2)**: items are PRE-RENDERED into `src/pages/gallery.html` from a scan of `content/gallery/` — **drop any image file (jpg/jpeg/png/webp/gif/svg) there and rebuild; no JSON to touch**. `loadGallery()` in build.js derives `caption` (filename humanized; optional `captions.json` sidecar in that folder overrides), auto-assigns a deterministic mixed-size layout from `GALLERY_LAYOUT` (`big`/`normal`/`small` × orientation cycling) + `data-speed` 1–4, copies files verbatim to `public/gallery/`, and throws on URL-unsafe filenames. Old `src/data/gallery.json` + generated `/img/gen/<seed>.svg` plates DELETED. `gallery.js` wires three motions: (1) reveal cascade (staggered `-active`, `(i%4)*.06s`), (2) **parallax on the filmstrip** — runs on desktop (page scroller) AND the mobile strip, its OWN rAF loop reads each `.gal-item` tile's LIVE `getBoundingClientRect()` every frame (feasible — tiles north of 20; the earlier "never re-read rect" rule became moot once we subtract our own applied offset: `centre = rect.left + width/2 - offs[i] - halfViewport`, so no feedback drift) and translates the **TILE** by `off = centre*(speed-1)*0.12` clamped ±260px (`.gal-item` transform writes via `offs[]`, `PAR_F`, `PAR_MAX`; speed-1 rail is never touched → `transform: none`). Offset applied as `offs[i] + (target-offs[i])*0.18` low-pass per frame — tames any pop when a tile enters the window and reads as silk, no quantised jumps; actual max single-frame step ≈14px. Screen velocity = scroll × `(1 + 0.12*(speed-1))` → higher speeds OVERTAKE the rail (layered depth); speed-4 (~15px mean velocity in the central tracking window) vs speed-2 (5) vs rail (0) — verified numerically. `prefers-reduced-motion` keeps the loop off; `.gal-item` may also have `transform: none` when untouched (identity is valid, not a bug), (3) click-to-zoom `-clicked` (5×, re-cascade). **No grayscale** (user preference). Layout: `.gal-sec { width:auto }` = a WIDE filmstrip child growing to `.gallery-track` `min-width:max-content`; tiles overlap via `margin: 0 -4vw 0 2vw`; scattered `margin-top` per nth-of-type. ≥900px it rides the page scroller (`main.scrollWidth > clientWidth`); below 900px `.sec { width:100% !important }` forces the track to a self-contained `overflow-x:auto` touch row (mobile rules in 08-pages.css 899px block). Click-zoom listens on `document` + `document.elementFromPoint()` because `horizontal.js`'s `setPointerCapture` redirects click targets to `<main>` — **keep that trick, don't regress to per-img listeners**. Verified `/tmp/opencode/gallery-parallax-verify.js` 9/9 + `gallery-strip-verify.js` 17/17 + regress-smoke 12/12.